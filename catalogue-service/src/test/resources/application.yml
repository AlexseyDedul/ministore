spring:
  datasource:
    url: jdbc:tc:postgresql:16:///ministore?TC_DAEMON=true
    username: ministore
    password: ministore
  boot:
    admin:
      client:
        enabled: false
  cloud:
    config:
      enabled: false
eureka:
  client:
    enabled: false